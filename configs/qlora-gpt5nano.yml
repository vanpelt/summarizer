# Axolotl QLoRA Configuration for Qwen3-8B
# Fine-tuning on gpt5nano dataset for summary and branch name generation
# Logs to W&B project "summarizer"

# Base model configuration
base_model: Qwen/Qwen3-8B
model_type: AutoModelForCausalLM
tokenizer_type: AutoTokenizer

# Chat template for Qwen3
chat_template: qwen3  # Uses <|im_start|> and <|im_end|> tokens

# QLoRA adapter settings
adapter: qlora
load_in_4bit: true

# LoRA hyperparameters
lora_r: 32
lora_alpha: 64
lora_dropout: 0.05
lora_target_modules:
  - q_proj
  - k_proj
  - v_proj
  - o_proj
  - gate_proj
  - down_proj
  - up_proj

# Quantization configuration
bnb_config_kwargs:
  bnb_4bit_quant_type: nf4
  bnb_4bit_compute_dtype: bfloat16
  bnb_4bit_use_double_quant: true

# Training hyperparameters
sequence_len: 2048
sample_packing: true
pad_to_sequence_len: true

# Batch size and gradient accumulation
# Effective batch size = micro_batch_size * gradient_accumulation_steps = 2 * 8 = 16
micro_batch_size: 2
gradient_accumulation_steps: 8
eval_batch_size: 2

# Training epochs and evaluation
num_epochs: 3
eval_steps: 25
save_steps: 50
logging_steps: 5

# Learning rate and scheduler
learning_rate: 0.0002
lr_scheduler: cosine
warmup_steps: 50

# Optimizer (memory efficient)
optimizer: paged_adamw_8bit
weight_decay: 0.01

# Performance optimizations
flash_attention: true
bf16: true
tf32: false
gradient_checkpointing: true

# Data configuration - using separate train/val files
datasets:
  - path: data/gpt5nano/train.jsonl
    type: chat_template
    field_messages: messages

# Validation dataset
test_datasets:
  - path: data/gpt5nano/val.jsonl
    type: chat_template
    field_messages: messages

# Output configuration
output_dir: ./models/qwen3-8b-gpt5nano-v1

# Saves
save_safetensors: true
save_total_limit: 3

# Weights & Biases logging
wandb_project: summarizer
wandb_entity: null  # Will use default W&B account
wandb_watch: false
wandb_log_model: checkpoint  # Log final model checkpoint to W&B
wandb_run_id: null  # Auto-generate run ID

# HuggingFace Hub (optional)
# hub_model_id: your-username/qwen3-8b-gpt5nano
# push_to_hub: false

# Special tokens (Qwen3 specific)
special_tokens:
  pad_token: "<|endoftext|>"

# Misc
seed: 42
strict: false
