# Axolotl QLoRA Configuration for Qwen3-8B
# Optimized for multi-job training on 128GB GPU

# Base model configuration
base_model: Qwen/Qwen3-8B
model_type: AutoModelForCausalLM
tokenizer_type: AutoTokenizer

# Chat template for Qwen3
chat_template: qwen3  # Uses <|im_start|> and <|im_end|> tokens

# QLoRA adapter settings
adapter: qlora
load_in_4bit: true

# LoRA hyperparameters
lora_r: 32
lora_alpha: 64
lora_dropout: 0.05
lora_target_modules:
  - q_proj
  - k_proj
  - v_proj
  - o_proj
  - gate_proj
  - down_proj
  - up_proj

# Quantization configuration
bnb_config_kwargs:
  bnb_4bit_quant_type: nf4
  bnb_4bit_compute_dtype: bfloat16
  bnb_4bit_use_double_quant: true

# Training hyperparameters
sequence_len: 2048
sample_packing: true
pad_to_sequence_len: true

# Batch size and gradient accumulation
# Effective batch size = micro_batch_size * gradient_accumulation_steps
micro_batch_size: 2
gradient_accumulation_steps: 8
eval_batch_size: 2

# Training epochs and evaluation
num_epochs: 3
eval_steps: 50
save_steps: 100
logging_steps: 10

# Learning rate and scheduler
learning_rate: 0.0002
lr_scheduler: cosine
warmup_steps: 100

# Optimizer (memory efficient)
optimizer: paged_adamw_8bit
weight_decay: 0.01

# Performance optimizations
flash_attention: true
bf16: true
tf32: false
gradient_checkpointing: true

# Data configuration
datasets:
  - path: data/processed/train.jsonl
    type: chat_template
    field_messages: messages

val_set_size: 0.1
# Alternatively, use separate validation file:
# test_datasets:
#   - path: data/processed/val.jsonl
#     type: chat_template

# Output configuration
output_dir: ./models/qwen3-8b-json-v1

# Saves
save_safetensors: true
save_total_limit: 3

# Logging and monitoring
wandb_project: qwen3-json-finetuning
wandb_entity: null  # Set to your W&B username
wandb_watch: false
wandb_log_model: false

# HuggingFace Hub (optional)
# hub_model_id: your-username/qwen3-8b-json
# push_to_hub: false

# Special tokens (Qwen3 specific)
special_tokens:
  pad_token: "<|endoftext|>"

# Misc
seed: 42
strict: false
